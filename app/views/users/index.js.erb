$("<%=j render @users %>").appendTo($("#users"));

<% if @users.next_page %>
    $('.pagination').html('<%= link_to_next_page(@users, 'Next Page', :remote => true) %>');
<% else %>
    $('.pagination').remove();
<% end %>

